'use strict';

/**
 * Convert TeiObjects into xml data
 */
class Tagger {
  constructor() {}

  /**
   * Analyse the given text for named entities
   * and return the
   * @param {string} text
   * @return Promise<Tag>
   */
  static tagText(text) {
    const res = text;
    return res;
  }
}

module.export = Tagger;
